@model Common.Entities.Propietario

<dl class="dl-horizontal">
    <dt>
        @Html.DisplayNameFor(model => model.PersonaId)
    </dt>

    <dd>
        @Html.DisplayFor(model => model.PersonaId)
    </dd>

    <dt>
        @Html.DisplayNameFor(model => model.Estado)
    </dt>

    <dd>
        @Html.DisplayFor(model => model.Estado)
    </dd>

</dl>
<div class="modal-footer">
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            <button type="button" onclick="PartialAction('@Url.Action("Edit", "Propietario", new { id = Model.Id })', 'DivForEdit', 'EditModal', 'DetailsModal');" class="btn btn-primary">Edit</button>
        </div>
    </div>
</div>